# คู่มือการใช้งาน Network Monitor

## การติดตั้ง

### ความต้องการของระบบ
- Python 3.7 ขึ้นไป
- pip (Python package manager)
- เบราว์เซอร์ที่รองรับ (Chrome, Firefox, Edge)

### ขั้นตอนการติดตั้ง

1. **ติดตั้ง Dependencies**
```bash
cd c:\Project\NW MonitorV0
pip install -r requirements.txt
```

2. **เริ่มต้นเซิร์ฟเวอร์**
```bash
python app.py
```

3. **เข้าใช้งานระบบ**
- เปิดเบราว์เซอร์ไปที่ `http://localhost:5000`

---

## การใช้งาน

### 1. Dashboard (หน้าแรก)

**ฟีเจอร์:**
- แสดงสถิติรวมของอุปกรณ์ทั้งหมด
- รายการอุปกรณ์พร้อมสถานะ
- กราฟ Response Time
- Activity Log แสดงกิจกรรมแบบ real-time

**การใช้งาน:**
- ดูสถิติได้ที่บริเวณด้านบน (Total Devices, Devices UP, Devices DOWN, Uptime)
- คลิก "Refresh" เพื่ือตรวจสอบสถานะทันที

### 2. Device Management (จัดการอุปกรณ์)

**เพิ่มอุปกรณ์ใหม่:**
1. คลิกปุ่ม "Add Device"
2. กรอกข้อมูล:
   - **Name**: ชื่ออุปกรณ์ (เช่น "Main Router")
   - **IP Address**: IP ของอุปกรณ์ (เช่น "192.168.1.1")
   - **Device Type**: ประเภทอุปกรณ์ (Router, Switch, Server, ฯลฯ)
   - **Location**: ตำแหน่งที่ตั้ง (ไม่บังคับ)
3. คลิก "Save Device"

**แก้ไขอุปกรณ์:**
1. คลิกปุ่ม ✏️ (Edit) ที่อุปกรณ์ที่ต้องการ
2. แก้ไขข้อมูล
3. คลิก "Save Device"

**ลบอุปกรณ์:**
1. คลิกปุ่ม 🗑️ (Delete)
2. ยืนยันการลบ

**ตรวจสอบสถานะทันที:**
- คลิกปุ่ม 🔄 (Check Now) เพื่อตรวจสอบสถานะอุปกรณ์ทันที

### 3. Network Topology (แผนผังเครือข่าย)

**ฟีเจอร์:**
- แสดงแผนผังเครือข่ายแบบ interactive
- สีของ node บอกสถานะ:
  - 🟢 **เขียว** = อุปกรณ์ออนไลน์ (UP)
  - 🔴 **แดง** = อุปกรณ์ออฟไลน์ (DOWN)
  - ⚪ **เทา** = ยังไม่ทราบสถานะ (Unknown)

**การใช้งาน:**
- **ลาก**: คลิกค้างที่ node แล้วลากเพื่อย้ายตำแหน่ง
- **ซูม**: ใช้ scroll wheel เพื่อซูมเข้า/ออก
- **คลิก node**: ดูข้อมูลอุปกรณ์
- **Fit to Screen**: ปรับมุมมองให้เห็นทั้งหมด
- **Refresh**: รีเฟรชแผนผัง

---

## การตั้งค่า

### ปรับ Monitoring Interval

แก้ไขไฟล์ `config.py`:

```python
PING_INTERVAL = 10  # วินาที (ค่าเริ่มต้น: 10)
PING_TIMEOUT = 2    # วินาที (ค่าเริ่มต้น: 2)
PING_COUNT = 3      # จำนวน ping ต่อครั้ง (ค่าเริ่มต้น: 3)
```

### เปลี่ยน Port

แก้ไขไฟล์ `app.py` บรรทัดสุดท้าย:

```python
socketio.run(app, debug=True, host='0.0.0.0', port=5000)
# เปลี่ยน port=5000 เป็นพอร์ตที่ต้องการ
```

---

## คำแนะนำ

### อุปกรณ์ที่แนะนำให้ Monitor

1. **Router หลัก** - เช่น 192.168.1.1
2. **Switch สำคัญ** - อุปกรณ์กระจายสัญญาณ
3. **Server** - เซิร์ฟเวอร์ต่างๆ
4. **Firewall** - อุปกรณ์รักษาความปลอดภัย
5. **DNS Server** - เช่น 8.8.8.8 (Google), 1.1.1.1 (Cloudflare)

### Best Practices

1. **ตั้งชื่อที่เข้าใจง่าย**: ใช้ชื่อที่บอกหน้าที่ชัดเจน
2. **ระบุ Location**: ช่วยในการค้นหาอุปกรณ์
3. **เลือก Type ที่ถูกต้อง**: ช่วยในการจัดกลุ่ม
4. **ตรวจสอบสถานะเป็นประจำ**: ดู Dashboard บ่อยๆ

---

## การแก้ปัญหา

### ปัญหา: เซิร์ฟเวอร์ไม่เริ่มทำงาน

**วิธีแก้:**
1. ตรวจสอบว่าติดตั้ง dependencies ครบแล้ว
2. ตรวจสอบว่าพอร์ต 5000 ว่าง (ไม่มีโปรแกรมอื่นใช้)
3. รัน `pip install -r requirements.txt` อีกครั้ง

### ปัญหา: อุปกรณ์แสดงสถานะ DOWN แต่จริงๆ ออนไลน์อยู่

**สาเหตุที่เป็นไปได้:**
1. อุปกรณ์บล็อก ICMP (ping)
2. Firewall บล็อกการ ping
3. IP address ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ IP address
2. เปิดใช้งาน ICMP response ที่อุปกรณ์
3. ตรวจสอบ firewall settings

### ปัญหา: Real-time updates ไม่ทำงาน

**วิธีแก้:**
1. รีเฟรชหน้าเว็บ
2. ตรวจสอบ Console ในเบราว์เซอร์ (F12)
3. ตรวจสอบว่า WebSocket เชื่อมต่อสำเร็จ

---

## ข้อมูลเพิ่มเติม

### โครงสร้างไฟล์

```
NW MonitorV0/
├── app.py              # Flask application หลัก
├── monitor.py          # Monitoring service
├── database.py         # Database layer
├── config.py           # Configuration
├── requirements.txt    # Dependencies
├── templates/          # HTML templates
│   ├── index.html
│   ├── topology.html
│   └── devices.html
└── static/
    ├── css/
    │   └── style.css
    └── js/
        ├── dashboard.js
        ├── topology.js
        └── devices.js
```

### Database

ข้อมูลเก็บในไฟล์ `network_monitor.db` (SQLite)

**Tables:**
- `devices`: ข้อมูลอุปกรณ์
- `topology`: ความสัมพันธ์ระหว่างอุปกรณ์
- `status_history`: ประวัติสถานะ

---

## การติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Console logs
2. ดู `walkthrough.md` สำหรับตัวอย่างการใช้งาน
3. ตรวจสอบ `implementation_plan.md` สำหรับรายละเอียดทางเทคนิค
